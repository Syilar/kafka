spring:
  kafka:
    bootstrap-servers: localhost:9092

app:
  kafka:
    orderTopic: "order-topic"
    orderStatusTopic: "order-status-topic"
    orderEventGroup: "order-event-group"

server:
  port: 8089